pipeline {
    agent any

    tools {
        git 'Git'
    }

    stages {
        stage('Clone Repo') {
            steps {
                git 'https://github.com/VETRI9876/Incident_Tracker.git'
            }
        }
    }
}
